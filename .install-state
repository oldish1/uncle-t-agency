format: 1
founder: Theo
os: windows
plan: pro
structure: one primary (Uncle T Agency, full context/), light satellites (Uncle T's cannabis, Rags to Riches, one page each). CHALES is the agency's flagship client, not a satellite.
steps:
  0: done
  1: done
  2: done
  3: done
  4: done
  5: done
  6: done
  7: pending
  8: pending
  9: pending
  10: pending
keys: firecrawl, supadata (both saved in .env; live test blocked by this cloud
  session's network policy, which only allows a short allowlist of outbound
  domains. Theo agreed to trust the setup and test both live next time he's
  in a normal Claude Code session.)
tools: google-calendar, gmail, google-drive (all confirmed live, adamst70@gmail.com). netlify not yet authorized. whatsapp/make.com/meta business manager have no connector, not covered.
runtime: ready (uv venv + requirements.txt installed, verified with the
  import check)
remote: https://github.com/oldish1/chales-website (branch claude/install-command-uvkm36)
notes: Running in a Claude Code cloud session, not the Desktop app. Git and the
  GitHub backup already exist (repo cloned remotely, pushed to the branch above),
  so Step 4 is mostly a walkthrough. Repo also holds pre-existing website photos
  and one video at the root (CHALES salon photos, Lexi's WhatsApp profile photo);
  leave them alone. Glaido voice dictation and Desktop connector panes are
  local-machine features - adapt those steps to what this remote session can reach.
  Business is Uncle T Agency (WhatsApp/AI automation for Cape Town small
  businesses, flagship product The Lexi System). CHALES Hair Boutique is the
  agency's live flagship client (Theo's daughter Chante's salon), not a separate
  business. Repo is still named chales-website; Theo wants it renamed to reflect
  Uncle T Agency but no rename tool is available here, he'll do it in GitHub
  settings. No repo-rename tool exists in this session's toolset.
  wife: explicitly excluded from the workspace, not part of the business.
  private/ now holds real margin detail and the family-motivation background
  Theo asked to keep out of shared context.
  Standing permission (given after Step 2): keep origin/main automatically
  synced with this install branch after each step, so a fresh session Theo
  opens always has working /prime and the rest of the commands. Merge
  claude/install-command-uvkm36 into main and push both after every step
  lands, no need to ask again each time.
